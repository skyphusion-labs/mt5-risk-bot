.venv/
src/*.egg-info/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
config.toml
journal.jsonl
journal.jsonl.1
journal.lock
journal.heartbeat
journal.tg_offset
journal.advice.json
journal.equity.json
journal.equity.json.tmp
HALT
.env
*.log
.DS_Store
agent/node_modules/
agent/.wrangler/
agent/.dev.vars

# Compiled MQL4 binaries. CI builds these and publishes them as artifacts tied to a
# commit sha (.github/workflows/mt4-compile.yml); a hand-compiled one in the tree is
# untraceable and must never be committed.
*.ex4
